export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080' // Fallback to localhost for dev
  // TODO: Check if can readd
  // apiUrl: process.env['BACKEND_API_URL'] || 'http://localhost:8080' // Fallback to localhost for dev
};

